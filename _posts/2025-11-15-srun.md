---
layout: post
title: "Shell inside a job"
comments: false
---

To access the shell inside a job, instead of SSHing into 
the node (in a case when one has multiple jobs in the same node).
```bash
srun --jobid=<jobid> --pty bash
```
